# Online-Quiz-Platform
This is a Python-based quiz application built with Streamlit and a MySQL database. It features separate interfaces for user and admin logins. Admins can manage quiz questions (add, view, and delete), while users can take quizzes on different subjects. It uses `subprocess` for page navigation.
